# vscode install
    pip install pylint
    pip install flake8
    pip install yapf

# install package
    # 图片裁剪 Pillow 
    pip install Pillow
    # pymysql 代替 mysql
    pip install pymysql

# git
    # fatal: no upstream configured for branch 'master'
    git branch --set-upstream-to=origin/master master